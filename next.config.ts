import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Socket.IO requires custom server, we'll handle it in phase 4
  // For now, keep default config clean
};

export default nextConfig;
